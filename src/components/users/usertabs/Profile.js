import React from 'react';

const Profile = ({ uid }) => {
    // Access the uid prop directly
    return (
        <>
            Profile component, User ID: {uid}
        </>
    );
};

export default Profile;
